# Copy to .env locally, or set these in Render's environment settings.
# Placeholder only. Use Supabase Connect > Session pooler (IPv4), URL-encode password.
DATABASE_URL=postgresql://postgres.PROJECT_REF:REPLACE_ME@POOLER_HOST:5432/postgres?sslmode=require
CORS_ALLOWED_ORIGINS=https://YOUR-FRONTEND.example
DEMO_MODE=true
LOCAL_DEMO=false
SESSION_HOURS=24
PORT=8000
