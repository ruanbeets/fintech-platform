# Copy to .env for local production-build checks. Vite dev also works without this file.
# On Render set this to the deployed HTTPS backend URL, then rebuild.
# Never put credentials in frontend environment variables.
VITE_API_BASE_URL=http://localhost:8000
